name: tunnel-hub

services:
  website:
    image: tunnel-hub-website
    container_name: tunnel-hub-website
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:${WEBSITE_HTTP_PORT:-11963}:80"
    restart: unless-stopped
